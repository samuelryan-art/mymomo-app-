import React from "react";

const Settings = () => {
  return (
    <div style={{ padding: "20px" }}>
      <h2>Settings</h2>
      <p>Manage your account, update profile, and configure preferences here.</p>
      {/* Later: add PIN setup, notification settings, and virtual card deletion */}
    </div>
  );
};

export default Settings;
